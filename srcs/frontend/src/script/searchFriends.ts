import { connectFunc, inputToContent, requestBody } from "./connections"

export function searchBar(query: string, publicUser: []) {
	console.log("Searching...")
	console.log("query is:", query)
	// comment here for testing
}