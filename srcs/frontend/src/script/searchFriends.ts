import { PubUserSchema } from "../pages/friends"

export function searchBar(query: string, publicUsers: PubUserSchema[]) {
	console.log("Searching...")
	console.log("query is:", query)
	console.log(publicUsers)
	// comment here for testing
}